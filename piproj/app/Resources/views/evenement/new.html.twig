{% extends 'base1.html.twig' %}

{% block body %}
    <script type="text/javascript">
        function dateTest() {
            var d1 = document.getElementById('evenementbundle_evenement_datedebut_day').value;
            var m1 = document.getElementById('evenementbundle_evenement_datedebut_month').value;
            var y1 = document.getElementById('evenementbundle_evenement_datedebut_year').value;

            var d2 = document.getElementById('evenementbundle_evenement_datefin_day').value;
            var m2 = document.getElementById('evenementbundle_evenement_datefin_month').value;
            var y2 = document.getElementById('evenementbundle_evenement_datefin_year').value;

            var date1 = new Date(y1 + "/" + m1 + "/" + d1);
            var date2 = new Date(y2 + "/" + m2 + "/" + d2);

            var today = new Date();
            console.log(today);
            date1 = date1.getTime();
            date2 = date2.getTime();

            if (date1 == date2) {
                alert('Dates are equal');
                return true
            } else {
                alert('Dates are not equal');
                return false;
            }
        }

           if( )
            ("evenementbundle_evenement_affiche").onclick(function () {
                if (dateTest()) {
                    console.log('bloquer');
                    event.preventDefault();
                } else {

                }
            })



    </script>

    <div id="lp-register">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">

            <div class="reg-form-container">
                <hr>
                <hr>
                <hr>
                <hr>
                <hr>
                <hr>
                <hr>
                <h1>Creation d'un évenement</h1>

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" class="btn btn-primary" value="Créer"/>
                {{ form_end(form) }}
            </div>
        </div>
        <div class="col-sm-3"></div>
    </div>
    {#   <ul>
         <li>
             <a href="{{ path('evenement_index') }}">Back to the list</a>
         </li>
     </ul>
    #}

{% endblock %}
